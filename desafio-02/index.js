"use strict";
//Treinando inferencia e annotation
const vr = 20;
//Convertendo o numero
const transform = vr.toString();
const View = `O numero digitado é ${transform}`;
console.log(View);
//-----------------------------
const min = 11;
const trsaw = min.toString();
const wia = `O numero a ser convertido é ${trsaw} e o ${transform}`;
console.log(wia);
