//Treinando inferencia e annotation

const vr: number = 20

//Convertendo o numero
const transform = vr.toString()

const View: string = `O numero digitado é ${transform}`

console.log(View)

//-----------------------------

const min: number = 11

const trsaw = min.toString()

const wia: string = `O numero a ser convertido é ${trsaw} e o ${transform}`

console.log(wia)